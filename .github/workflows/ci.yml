name: 'CI'
description: 'Validates sentry-shared-kafka devservices config'

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: getsentry/action-validate-devservices-config@03fcddd694b4c24209df7eb32eb7edf761dc5afb

