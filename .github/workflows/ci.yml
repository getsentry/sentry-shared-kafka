name: 'CI'
description: 'Validates sentry-shared-kafka devservices config'

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: getsentry/action-validate-devservices-config@6f30f272608bfd994e6aa36af6b1e200c1408062

