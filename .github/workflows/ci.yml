name: 'CI'
description: 'Validates sentry-shared-kafka devservices config'

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: getsentry/action-validate-devservices-config@e162f810143a4d4ed9f82bd8b93181219e8b8dd6

